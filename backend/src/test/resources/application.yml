spring:
  datasource:
    url: jdbc:h2:mem:saas_test_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: false

app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    access-expiration-ms: 900000
    refresh-expiration-ms: 604800000
  ai:
    image:
      provider: mock
    multimodal:
      provider: mock
    web-search:
      provider: mock

logging:
  level:
    com.saasplatform: WARN
    org.springframework: WARN
